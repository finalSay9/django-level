# Core
Django>=5.1,<5.2
psycopg[binary]>=3.2,<4     # much faster & easier than psycopg2
gunicorn>=23.0,<24
whitenoise>=6.8     
Pillow>=10.0.0        # static files in production
djangorestframework>=3.15,<4.0

# Development / recommended
django-debug-toolbar>=4.4
python-dotenv>=1.0

# Optional – add when you need them
# redis>=5.2
# celery>=5.4
# django-celery-results>=2.5
# django-celery-beat>=2.7